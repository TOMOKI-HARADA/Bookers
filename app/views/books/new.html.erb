<h1>新規投稿</h1>
<%= form_with model: @book, local:true do |f| %>
<h4>タイトル</h4>
<%= f.text_field :title %>

<h4>本文</h4>
<%= f.text_field :body %>
<%= f.submit '保存' %>
<% end %>
<%= link_to 'back', books_path %>
 <%= link_to 'show', show_book_path(@book.id) %>